# Copy this file to .env and fill in your values.
# Never commit .env to version control.

COHERE_API_KEY=your_key_here

# Optional: override the committed app/prompts/system_prompt.md with a
# production-hardened prompt. Set this as a Render secret to keep the live
# prompt out of version control. Leave unset in development.
# SYSTEM_PROMPT=
